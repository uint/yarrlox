fun fun_gen() {
    var x = 5;

    // this function should capture the local x = 5 variable
    fun print_x() {
        print x;
    }

    return print_x;
}

var fn = fun_gen();

fn();    // 5
print x; // nil, because `x = 5` isn't in scope here
